apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    external-secrets.io/component: controller
  name: hexes.generators.external-secrets.io
spec:
  group: generators.external-secrets.io
  names:
    categories:
    - external-secrets
    - external-secrets-generators
    kind: Hex
    listKind: HexList
    plural: hexes
    singular: hex
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Hex generates a random hexadecimal string based on the
          configuration parameters in spec.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HexSpec controls the behavior of the hex string generator.
            properties:
              length:
                default: 16
                description: |-
                  Length of the hex string to be generated.
                  Defaults to 16 (8 bytes)
                type: integer
              prefix:
                description: Prefix specifies an optional prefix to add to the hex
                  string (e.g., "0x")
                type: string
              uppercase:
                default: false
                description: Uppercase specifies whether to use uppercase letters
                  (A-F) instead of lowercase (a-f)
                type: boolean
            required:
            - length
            - uppercase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
