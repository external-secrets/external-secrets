apiVersion: generators.external-secrets.io/v1alpha1
kind: QuayAccessToken
metadata:
  name: my-quay-token
  namespace: default
spec:
  url: "quay.io"
  robotAccount: "dronenb+testing"
  serviceAccountRef:
    name: "default"
    namespace: "default"
