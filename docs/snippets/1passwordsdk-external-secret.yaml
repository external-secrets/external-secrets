---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: my-cert
spec:
  secretStoreRef:
    kind: SecretStore
    name: 1password-sdk
  target:
    creationPolicy: Owner
  data:
  - secretKey: key
    remoteRef:
      key: remote-key
      property: property
