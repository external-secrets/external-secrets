apiVersion: generators.external-secrets.io/v1alpha1
kind: Hex
metadata:
  name: hex-example
spec:
  # Generate a 32-character hex string
  length: 32
  # Use uppercase letters (optional)
  uppercase: true
  # Add "0x" prefix (optional)
  prefix: "0x"
