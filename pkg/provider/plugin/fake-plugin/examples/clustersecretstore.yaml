apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: fake-plugin-cluster-store
spec:
  provider:
    plugin:
      endpoint: "tcp://fake-plugin-service:9999"
      timeout: "15s"
