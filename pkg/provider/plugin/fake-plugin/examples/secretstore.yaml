apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: fake-plugin-store
  namespace: default
spec:
  provider:
    plugin:
      endpoint: "unix:///tmp/fake-plugin.sock"
      timeout: "10s"
